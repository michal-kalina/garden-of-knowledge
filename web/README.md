# web

Next.js frontend: document upload with live pipeline statuses, streaming chat,
and clickable [n] citations that light up their source card.

Dev: `npm install && npm run dev` (expects the API on localhost:8080; override
with `API_URL`). In docker-compose it runs on http://localhost:3000.
